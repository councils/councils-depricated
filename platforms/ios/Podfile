# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'

target 'Councils' do
    pod 'Firebase', '>= 2.3.3'
end

target 'Councils-watch' do
    pod 'Firebase', '>= 2.3.3'
end

target 'Councils-watch Extension' do
    pod 'Firebase', '>= 2.3.3'
end

